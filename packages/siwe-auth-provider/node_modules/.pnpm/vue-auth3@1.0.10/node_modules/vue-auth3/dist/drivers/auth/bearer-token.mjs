import {
  defineAuthDriver
} from "../../chunk-QALVUXMO.mjs";
import "../../chunk-MI2N2CU6.mjs";

// src/drivers/auth/bearer-token.ts
var bearer_token_default = defineAuthDriver({
  request(auth, options, token) {
    options.headers["Authorization"] = "Bearer " + token;
    return options;
  },
  response(auth, res) {
    const token = res.data.token;
    if (token) {
      const i = token.split(/Bearer:?\s?/i);
      return i[i.length > 1 ? 1 : 0].trim();
    }
    return null;
  }
});
export {
  bearer_token_default as default
};
//# sourceMappingURL=bearer-token.mjs.map